# TRINETRA cv-engine — AI/CV pipeline for Sentinel CCTV feeds
# Python 3.11+
#
# ⚠ ultralytics and easyocr declare the GUI `opencv-python` wheel, which can
# overwrite cv2 with a build that requires libGL.so.1 on headless servers.
# After installing this file, run:
#     python ../scripts/ensure_headless_opencv.py
# The helper removes GUI OpenCV and repairs the cv2 installation without
# redownloading the headless wheel on every run.

# Core
numpy>=1.26.0
httpx>=0.27.0

# Detection (YOLO11) — install CPU-only torch first on CPU machines:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision>=0.17.0
ultralytics>=8.3.0

# ANPR / OCR
easyocr>=1.7.1
rapidocr-onnxruntime  # offline OCR for the ANPR stage (models bundled in the wheel)

# Performance metrics (benchmark script)
psutil>=5.9.0

# Tests
pytest>=8.1.0

# Declared explicitly; run scripts/ensure_headless_opencv.py after pip resolves
# transitive dependencies so this provider wins the cv2 import.
opencv-python-headless>=4.9.0   # headless build for servers; no libGL required
